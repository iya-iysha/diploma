type RegProps = {
    name: string;
    email: string;
    password: string;
}

type LogProps = {
    name: string;
    password: string;
}

export const registrateUser = (body:RegProps) => {
    //
}

export const logUser = (body:LogProps) => {
    //
}